---
title: "業務効率化入門"
author: "宮森祥行"
date: last-modified
format:
  html:
    code-copy: true
    date-format: "YYYY年M月D日"
    toc: true
    embed-resources: true
bibliography: references.bib
---
以下はmacOSでの話である．業務効率化と大げさに言ってしまったが，ここでは外付けのデバイスには一切触れず，扱うのはソフトの話のみである．当たり前だが一番の業務効率化はブラインドタッチを覚えることである．個人的にはe-typingというサイトがおすすめであり，例えば基礎練習の練習1-4をやればすぐにできるようになるはず．

# システム設定

システム設定を開き，以下の見出しに移動して設定をしよう．

## キーボード

入力リソースの編集ボタンを押す．「日本語-ローマ字入力」を選択し，英字にチェックを入れる．その後「日本語-ローマ字入力」の上の「ABC」を選択して，`-`ボタンを押して削除する．再び「日本語-ローマ字入力」に戻ると設定を自由に変更できるようになる．私は以下の設定をした．

- 句読点の種類を「．と，」に変更
- "¥"キーで入力する文字を「\\(バックスラッシュ)」に変更
- 「🌐キーを押して」を「何もしない」に変更

全ての入力ソースで

- 「英字入力中にスペルを自動変換」をオフ
- 「文頭を自動的に大文字にする」をオフ
- 「提案された返事を表示」をオフ
- 「スペースバーを2回押してピリオドを入力」をオフ

## デスクトップとDock

- 「Dockを自動的に表示/非表示」をオン
- 「アプリの提案と最近使用したアプリをDockに表示」をオフ
- サイズを最大に変更
- ホットコーナーボタンをクリックして右下の「クイックメモ」を「-」に変更

Dockからいらないものを消す．
ちなみに私の現在のDockはFinder, Safari, VSCode, ターミナル, システム設定，アクティビティモニタ，ダウンロード，ゴミ箱である．(アクティビティモニタは，プログラムなどを実行しているが画面上は変化がない時に，ちゃんと動いているか確認するのに割と開く)


## トラックパッド

使い方が解説してある．非常に便利なので全てマスターしよう．

トラックパッドに関連した設定：

- 大きなディスプレイがあればそれに越したことはないが，ノートパソコンのディスプレイは小さいので分割したくない．私は現在デスクトップを5枚用意して各アプリを一面いっぱいに表示し，3本指でスワイプして切り替えている．ディスプレイを追加するには3本指で上にスワイプして右端の$+$をクリック．私には5枚が多すぎず少なすぎずちょうど良い．
- 通知センターもカスタマイズしておこう．現在私は時計とカレンダーと天気のウィジェットがある．






# ショートカット

ここでは使いそうなものだけを厳選して紹介する．

## テキスト編集

- `command + C` : コピー(Copy)
- `command + X` : カット(ハサミっぽい?)
- `command + V` : ペースト
- `shift + option + command + V` : フォーマットなしでテキストのみペースト
- `command + A` : 全選択(All)
- `command + Z` : 元に戻す
- `command + shift + Z` : 戻しすぎた操作をキャンセル

:::callout-tip
これらはテキストに限らず，ファイルに対しても有効である．
:::

## ファイル編集

- `command + backspace` : ゴミ箱に入れる (まさかゴミ箱までドラッグ&ドロップしてないですよね?)
- `command + P` : 印刷(Print)
- `command + D` : 複製(Duplicate)


## アプリ操作

- `command + Q` : 完全に終了する(Quit)
- `command + W` : ウィンドウを閉じる(Window) (まさかあんな小さい赤丸ボタンを押しに行ってないですよね?)
- `command + M` : ウィンドウを最小化する(Minimize) (こちらは黄色丸ボタン．私はそもそもあまり使うことはない)
- `control + command + F` : フルスクリーンにする(Full screen) (これが緑丸ボタン)
- `command + T` : 新規のタブを開く(Tab)

## スクリーンショット

- `shift + command + 3` : 画面全体のスクショをデスクトップに保存
- `control + shift + command + 3` : 画面全体のスクショをクリップボードに保存(AIとの会話で使い捨てるとき用)
- `shift + command + 4` : 一部を選択してスクショをデスクトップに保存
- `control + shift + command + 4` : 一部を選択してスクショをクリップボードに保存
- `shift + command + 5` : 録画する(厳密には違うが私は録画にしか使わない)

## VSCode

これはちょっと多いし複雑で説明しづらいので例えば[@shimabu]を見てほしい．

## オリジナルショートカット

ショートカットというアプリがある．上の$+$アイコンをクリックするとショートカットが作成できる．
検索窓に「アプリ」と入れると例えば「アプリを開く」という操作があったり，「URL」と入れると「URL」と「URLを開く」という操作があったり色々．
これらを左にドラッグ&ドロップするだけである．
完成したらⓘをクリックして「キーボードショートカットを追加」ボタンをクリックし，割り当てたいコマンドを実際に打てば良い．
しかしこれが案外干渉してしまうのである．干渉を避けるにはたくさんのキーを組み合わせた複雑なものにするしかないが，そんなの覚えてられない．
そこで便利なのがKarabiner-Elementsである．インストールしよう．
```zsh
brew install --cask karabiner-elements
```
ポップアップに従って設定を済ませる．
その後Complex Modificationsタブをクリックする．まずは`Add predefined rule`ボタンをクリックし好きなものをEnableにしよう．
私は`Change spacebar to left_shift. (Post spacebar if pressed alone)`をEnableにした．Shiftを押すたびに片手のホームポジションが崩れるストレスから解放された．
これだけでもKarabiner-Elementsを導入する価値がある．
さて，本題のショートカット干渉問題だが，`Add your own rule`で人間が実際に押すキーとコンピュータが押されたと認識するキーを帰ることができる．例えば`control + A`を押したらそれは`shift+ control + option + command + A`を押したことにできるのである．追加しようとすると.jsonのサンプルファイルを見せてくれるので，自分がやりたい変換と共にAIに投げればいいように書いてくれるはずである．

さて私は現在どうしているのかというと，色々試してはみたのだが，結局先ほどのスペースキーをShiftに変えただけに落ち着いてしまった．
ショートカットは最終的には自分の記憶容量とのトレードオフであり，今のところショートカットしたいと思うほど不便なものはなかった．
ショートカットキーはかわいいものだが，業務効率化あるあるとして，楽しくなってマニアックなものに手をつけ始めると，最初の設定にかかる時間がどんどん増え元が取れなくなっていくというものがある．程々にしておこう．


# Shellをカスタマイズ

zshはdefaultだと毎回表示される`username@hostname`は全くいらないし，テキストも単色で見づらく，VSCodeのように色分けして欲しい．
他の人がいい感じもものを作ってくれているのでそれを使わせてもらおう．

まずは[@iceberg]に従ってIcebergをインポートしよう．そのままフォントもMenlo Regular 18に変更しよう．
ターミナルは背景を透かすのがクールだと思われているみたいだが，私は見にくいだけな気がして変更していない．
また，カーソルが「ブロック」なのが地味に嫌だったのでカーソルから「| 垂直バー」と「点滅カーソル」を選択した．

次に[@dasai]に従ってプロンプトの設定を変更しよう．[@dasai]は微妙に読みづらいのでほとんど同じ内容になるがここにも書いておく．

ではPreztoというフレームワークを導入する．
```zsh
git clone --recursive https://github.com/sorin-ionescu/prezto.git "${ZDOTDIR:-$HOME}/.zprezto"
```
次に初期設定を行う．
```zsh
setopt EXTENDED_GLOB
for rcfile in "${ZDOTDIR:-$HOME}"/.zprezto/runcoms/^README.md(.N); do
  ln -s "$rcfile" "${ZDOTDIR:-$HOME}/.${rcfile:t}"
done
```
zshにその設定を読み込ませる．
```zsh
echo 'source "${ZDOTDIR:-$HOME}/.zprezto/init.zsh"' >> ~/.zshrc
```

次に`nano ~/.zpreztorc`を開いたら，
```zsh
#
# General
#

# Set case-sensitivity for completion, history lookup, etc.
# zstyle ':prezto:*:*' case-sensitive 'yes'

# Color output (auto set to 'no' on dumb terminals).
zstyle ':prezto:*:*' color 'yes'

# Add additional directories to load prezto modules from
# zstyle ':prezto:load' pmodule-dirs $HOME/.zprezto-contrib

# Allow module overrides when pmodule-dirs causes module name collisions
# zstyle ':prezto:load' pmodule-allow-overrides 'yes'

# Set the Zsh modules to load (man zshmodules).
# zstyle ':prezto:load' zmodule 'attr' 'stat'

# Set the Zsh functions to load (man zshcontrib).
# zstyle ':prezto:load' zfunction 'zargs' 'zmv'

# Set the Prezto modules to load (browse modules).
# The order matters.
zstyle ':prezto:load' pmodule \
  'environment' \
  'terminal' \
  'editor' \
  'history' \
  'directory' \
  'spectrum' \
  'utility' \
  'completion' \
  'history-substring-search' \
  'prompt'
```
という部分の最後を
```zsh
# The order matters.
zstyle ':prezto:load' pmodule \
  'environment' \
  'terminal' \
  'editor' \
  'history' \
  'directory' \
  'spectrum' \
  'utility' \
  'completion' \
  'syntax-highlighting' \
  'history-substring-search' \
  'autosuggestions' \
  'prompt'
```
に書き換えよう．また，
```zsh
#
# Prompt
#

# Set the prompt theme to load.
# Setting it to 'random' loads a random theme.
# Auto set to 'off' on dumb terminals.
zstyle ':prezto:module:prompt' theme 'sorin'
```
という部分を探し，`sorin`を`pure`に書き換えよう．
終わったら.zpreztorcを閉じてターミナルに再ログインすればイケてるプロンプトになっているはずである．

# VSCodeをカスタマイズ

ターミナルはdefaultがダメダメなので絶対に設定し直した方が生産性が上がると思うが，VSCodeはdefaultが結構いいので，生産性というよりはカスタマイズしてやったぜという気分の問題だと思う．例えば[@vscode2021]や[@vscode2024]といった記事がある．気に入ったものに設定すれば気分も少し上がるだろう．

私が今回変更したことは
```zsh
brew install font-udev-gothic-nf
```
でフォントをインストールしてsettings.jsonに
```zsh
// フォントを変更
"editor.fontFamily": "'UDEV Gothic 35NFLG'",
"editor.fontLigatures": true,
```
と書き込む．これで`<=`や`!=`が`≤`や`≠`のように表示されるようになり少し見やすい．また，VSCode内のターミナルも先ほどの設定が反映されて見やすくなっているはずだが，カーソルは設定し直してやる必要がある．
```zsh
// ターミナルのカーソルも垂直バーにして点滅させる
"terminal.integrated.cursorStyle": "line",
"terminal.integrated.cursorBlinking": true,
```

他には，気分の問題だが，
```zsh
// カッコを線で繋ぐ
"editor.renderLineHighlightOnlyWhenFocus": true,
```
JetBrains Icon Themeをインストールして，
```zsh
// アイコンを変更
"workbench.iconTheme": "vscode-jetbrains-icon-theme-2023-auto",
```
を設定してみた．